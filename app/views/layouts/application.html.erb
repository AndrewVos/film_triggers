<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <% meta_tag_defaults = { site: 'filmtriggers.com', title: 'Community-curated list of film triggers', reverse: true } %>
    <%= display_meta_tags(meta_tag_defaults) %>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= root_path %>">
            filmtriggers.com
          </a>
        </div>

        <div class="collapse navbar-collapse" id="navbar">
          <p class="navbar-text">
            the community-curated film trigger list
          </p>
          <ul class="nav navbar-nav navbar-right">
            <li>
              <%= link_to 'Contact', 'https://twitter.com/AndrewVos' %>
            </li>
            <% if signed_in? %>
              <%= active_link_to "Account", edit_user_registration_path, wrap_tag: :li %>
              <%= active_link_to "Sign Out", destroy_user_session_path, wrap_tag: :li, method: :delete %>
            <% else %>
              <%= active_link_to "Sign In", new_user_session_path, wrap_tag: :li %>
              <%= active_link_to "Sign Up", new_user_registration_path, wrap_tag: :li %>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <% if flash.any? %>
      <div class="container-fluid">
        <div class="row">
          <div class="col-xs-12">
            <% flash.each do |key, value| %>
              <div class="<%= flash_class(key) %>">
                <%= value %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>

    <div class="container-fluid">
      <div class="row">
        <div class="col-xs-12">
          <%= yield %>
        </div>
      </div>
    </div>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-443109-17', 'auto');
      ga('send', 'pageview');

    </script>
  </body>
</html>
